// ============================================
// CONFIG.JS
// Flags globais do projecto
// ============================================

const config = {
  appMode: false,        // true quando for app/PWA
  enableBlog: true,
  enableNewsletter: true,
  enableQRCode: true,
};

window.config = config;
