<!-- PARTE TÉCNICA DO PROJETO ================

Contexto: Projecto HIIT-Gym — PWA em vanilla JS/HTML/CSS deployado em Cloudflare Pages (hiit-gym.pages.dev), repo github.com/onitech38/HIIT-Gym, Supabase para auth/DB.

═══════════════════════════════════
ESTADO ACTUAL DA ARQUITECTURA
═══════════════════════════════════

FICHEIROS PRINCIPAIS:
• supabase.js → window.supabaseClient (cliente partilhado)
• js/global.js → fonte única de auth, splash, nav, boot()
• js/script.js → index.html (welcome modal, mapa, modalidades, carrossel)
• js/user.js → perfil do utilizador
• js/modalidades.js → página de modalidades e inscrições
• js/blog.js → blog com artigos
• admin/admin.js → painel de administração
• sw.js → service worker
• js/data/ → config.js, data-gym.js, data-actividades.js, data-blog.js

CONTRATO GLOBAL (fonte única de verdade):
• window.supabaseClient → cliente Supabase
• window.currentUser → null | User (definido pelo global.js antes de app:ready)
• app:ready → evento que todas as páginas ouvem para iniciar
• actualizarNav() → exportada pelo global.js
• ini(str) → exportada pelo global.js
• getSession() → chamado UMA vez em global.js boot(), nunca nos page scripts

SPLASH SCREEN (CSS puro, zero JS):
• html::before + html::after no <head> de cada página
• global.js adiciona classe html.splash-done para fade out
• Máx 1.5s via safetyTimer

SERVICE WORKER:
• CACHE = 'hiitgym-v5'
• JS → nunca em cache (network only)
• HTML → network-first
• CSS/imagens → cache-first
• skipWaiting() no install + message listener para SKIP_WAITING
• global.js envia postMessage('SKIP_WAITING') no boot()

========================================== -->
<!-- ESTRUTURA DE FICHEIROS ==================

HIIT Gym/(RAIZ)
|
|__ admin
|   |__ admin.css
|   |__ admin.html
|   |__ admin.js
|
|
|__ blog
|   |__ blog.css
|   |__ blog.html
|
|
|__ functions
|   |__ api
|       |__ chat.js
|       |__ send-email.js
|       |__ stripe-checkout.js
|       |__ stripe-portsl.js
|       |__ stripe-webhook.js
|
|
|__ js
|   |__ data
|   |   |__ config.js
|   |   |__ data-atividades.js
|   |   |__ data-blog.js
|   |   |__ data-gym.js
|   |
|   |
|   |__ blog.js
|   |__ email.js
|   |__ global.js
|   |__ modalidades.js
|   |__ script.js
|   |__ user.js
|   
|
|__ modalidades
|   |__ modalidades.css
|   |__ modalidades.html
|
|
|__ partials
|   |__ footer.html
|   |__ nav.html
|
|
|__ src ( Conteúdo multimédia )
|
|
|__ user
|   |__ user.css
|   |__ user.html
|
|
|__ _headers
|__ chat.css
|__ chat.js
|__ index.html
|__ manifest.json
|__ style.css
|__ supabase.js
|__ sw.js

========================================== -->
<!-- MEMÓRIA: ================================
MEMÓRIA:

Purpose & context
José is building HIIT-Gym, a Portuguese-language fitness gym website deployed on Cloudflare Pages via the onitech38/HIIT-Gym GitHub repository. The project is a PWA built with vanilla HTML, CSS, and JavaScript, using Supabase for authentication and database, and an Anthropic API proxy via Cloudflare Pages Functions for a chat assistant widget.
José works with Claude as a senior front-end/back-end collaborator. Established coding conventions:

Pure CSS Nesting where hierarchy mirrors HTML structure
@layer usage for animations, media queries, and variables
Minimal JavaScript; warn when JS is being introduced
Simplest and cleanest solutions preferred
Step-by-step explanations for each change
Avoid JS when CSS can handle it

José has been direct about expecting thorough diagnosis before solutions are proposed — he explicitly called out repeated misdiagnoses and asked Claude to evaluate all possibilities before acting.

Current state
The site is live on Cloudflare Pages. Recent sessions resolved a series of cascading production bugs following a refactor that introduced global.js for shared functions. Key resolved issues include:

data.js and global.js load order fixed in index.html (must precede script.js)
Supabase CDN pinned to @2.49.4 across all four HTML files to avoid cached version incompatibility with newer key formats
functions/api/chat.js fixed: replaced destructured const { request, env } = context with direct context.env / context.request access to resolve Cloudflare bundler scope errors
Dead code and duplicate data declarations (coaches, modalidadesData) removed from script.js
Blog auth guard (pre-Supabase localStorage redirect) removed
Supabase handle_new_user trigger rebuilt with COALESCE to accept both camelCase and snake_case metadata fields
Service Worker cache bumped to v3.1 with corrected asset paths

Pending / unconfirmed at last session end:

The chat.js fix (replacing the destructured context pattern) had been committed but not yet confirmed live in production
ANTHROPIC_API_KEY environment variable needs verification that it is set under the Production scope (not Preview only) in Cloudflare Pages settings


On the horizon

Confirming the chat assistant is fully functional end-to-end in production (API key scope, proxy routing, response delivery)
Chat feature gates fitness/nutrition advice behind Standard/Premium plan via profiles.plan column — may need further testing
Conversation history: stored in Supabase for authenticated users, localStorage for anonymous users — integration stability to verify


Key learnings & principles

Cloudflare Pages Functions scope issue: Destructuring context at the top level (const { env } = context) causes bundler scope errors; always access context.env and context.request directly inside the handler
GitHub file editing pitfall: Using GitHub's web editor without selecting all (Ctrl+A) before pasting appends content, causing duplicate export errors — always replace the full file
Service Worker cache as a debugging suspect: Stale SW cache has caused multiple misdiagnoses; cache version should be bumped and SW cleared when deploying fixes
Cloudflare Pages vs. Workers: Environment variables must be set in the Pages project, not a separate Worker — easy to misconfigure via the dashboard
Supabase CDN floating tags (@2 without a pinned version) can resolve to cached incompatible builds; always pin to a specific version
Commit verification before diagnosing: Several bugs were caused by the correct file never having been committed — always confirm the repo state matches expectations before assuming a logic error
CSS nesting and DOM structure must align: CSS nesting hierarchy should reflect actual rendered DOM, not assumed structure — validate against JS-rendered output when dynamic elements are involved


Approach & patterns

José uploads files at the start of sessions for Claude to audit before making changes
Fixes are delivered as complete, downloadable file outputs (not diffs)
Bugs are worked through sequentially by priority, confirmed by José before moving on
Production issues are debugged by cross-referencing browser console errors, Cloudflare deployment logs, and repository state
Rollbacks to last stable deploy are used as a stabilisation strategy during deployment crises


Tools & resources

Cloudflare Pages — hosting and Pages Functions (serverless proxy)
Supabase — authentication, user profiles, chat history storage
Anthropic API — powers the chat assistant widget (proxied server-side)
GitHub (onitech38/HIIT-Gym) — source repository with Cloudflare CI/CD integration
Service Worker (sw.js) — PWA caching, versioned manually

========================================== -->
<!-- INSTRUÇÕES: =============================
INSTRUÇÕES:
Pretendo que:
- sejas um ajudante senyor de front-end/back-end web dev;
- entregues o resultado mais simples e limpo;
- NESTING puro:que a hierarquia seja respeitada tal como está estruturada no html no mesmo - IMPORTANTE!!!
- animações, @media queries, variaveis ... tudo nos respectivos @layers - IMPORTANTE!!!
- que cada processo seja explicado passo a passo;
- tentes evitar a utilização de JS(mas informes no fim caso seja a melhor opção e 
  perguntes se quero ver/que seja implementada);
========================================== -->
<!-- O QUE FALTA: ============================

3. Resend (emails transaccionais, plano gratuito 3000/mês)
   • Confirmação de inscrição em modalidade
   • Boas-vindas no signup
   • Notificação ao admin de nova inscrição pendente
   • Confirmação quando admin aceita inscrição
4. pagamentos podem ser feitos pelo paypal, visa, mastercar, (o que conseguir simular FREE)
5. Theme toggle - light dark mode
6. os utilizadores poderem abrir sessão com conta google, etc..
8. Fase de polimento final
   • Treinos no perfil (adicionar/sincronizar)
   • QR Code (testar geração)
   • Admin — mostrar email real dos membros
   • Testes completos novo utilizador do zero

========================================== -->


<!-- Prompt para trabalho atual ==============
==== 30 / 04 / 2026 ==========================

PROMPT DE CONTINUAÇÃO — HIIT-Gym PWA
Contexto: Sou o developer do HIIT-Gym Montijo (onitech38/HIIT-Gym), uma PWA vanilla JS/HTML/CSS deployed no Cloudflare Pages (hiit-gym.pages.dev). Backend: Supabase auth/DB. Pagamentos: Stripe. Email: Resend. AI: Anthropic.

✅ FEITO E DEPLOYED
Infraestrutura:

Supabase auth + DB (profiles, enrollments, trainings, chat_history)
SW v5 (Network Only para JS, fix do "Wait" state)
global.js com arquitectura app:ready + window.currentUser
_headers com no-store para todos os JS

Stripe (100% funcional):

Checkout end-to-end (index → Stripe → user)
Webhook → Supabase (plan actualizado)
Billing Portal
Price IDs (Área Restrita): Básico price_1TDlVH77sfEVvt5IUrlmnFp2, Standard price_1TDlVh77sfEVvt5INOWoW8D9, Premium price_1TDlWC77sfEVvt5It1l75JNO

Páginas funcionais:

index.html — home, planos com checkout, destaque plano activo
user/user.html — dashboard, treinos, modalidades, perfil, QR, plano+portal
admin/admin.html — inscrições pendentes, membros (com badges de modalidades), tab Modalidades (accordion por modalidade com membros), gestão de planos
modalidades/modalidades.html — cards, inscrição 2 passos, fix getStatus (cancelled→disponivel)
blog/blog.html — gated (só autenticados)

Resend (deployed, NÃO testado ainda):

functions/api/send-email.js — REST API, sem npm, usa context.env.RESEND_API_KEY
js/email.js — 3 funções globais: emailBoasVindas, emailAdminNovaInscricao, emailInscricaoConfirmada
email.js carregado em index.html, modalidades.html, admin/admin.html
Coluna email adicionada à tabela profiles
SQL executado: UPDATE profiles p SET email = u.email FROM auth.users u WHERE p.id = u.id AND p.email IS NULL
RESEND_API_KEY no Cloudflare (estava Plaintext, mudar para Secret)
Sender: onboarding@resend.dev (sem domínio verificado — só envia para onitech38@gmail.com)


🔴 POR FAZER (por ordem)
1. Testar Resend ← começar aqui

Verificar se RESEND_API_KEY está como Secret no Cloudflare
Testar email de boas-vindas (criar conta nova)
Testar notificação admin (submeter inscrição em modalidade)
Testar confirmação membro (admin confirmar inscrição no painel)

2. Admin inbox/reply — messages + UI ← segunda tarefa

Criar tabela messages no Supabase:

sqlCREATE TABLE messages (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  from_user_id uuid REFERENCES profiles(id),
  to_user_id uuid REFERENCES profiles(id),
  subject text NOT NULL,
  body text NOT NULL,
  read boolean DEFAULT false,
  created_at timestamptz DEFAULT now()
);
ALTER TABLE messages ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Admin vê tudo" ON messages FOR ALL USING (
  EXISTS (SELECT 1 FROM profiles WHERE id = auth.uid() AND is_admin = true)
);
CREATE POLICY "User vê as suas" ON messages FOR SELECT USING (
  from_user_id = auth.uid() OR to_user_id = auth.uid()
);

Nova tab "Mensagens" no admin com lista de membros + botão "Enviar mensagem"
Modal de compose (para, assunto, corpo)
Enviar também por email via /api/send-email
No user/user.html — tab ou badge de mensagens recebidas

3. Chat → Anthropic (rápido, 1 ficheiro)

functions/api/chat.js ainda está em OpenAI — reverter para Anthropic
ANTHROPIC_API_KEY já está no Cloudflare

4. Login social Google (Supabase OAuth)
5. Pagamentos adicionais (PayPal via Stripe)
6. Light/dark theme toggle (UI já preparada)
7. SEO (meta tags, og:image)
8. Polimento final

⚙️ ARQUITECTURA TÉCNICA

Cloudflare Pages Functions: NUNCA usar const { request, env } = context — sempre context.env.X e context.request directamente
Supabase: nunca .catch() em queries — usar try/catch. signOut() deve ser awaited
SW: cache hiitgym-v5, JS = Network Only
Admin UUID: 0be84215-d1cd-4f75-bc7f-1fd4b5e21d22
Supabase project: kwvvoisnmqghkzllgegx
Resend sender: onboarding@resend.dev; admin email: onitech38@gmail.com


🎨 CSS/DESIGN

Custom properties: --clr-1 (branco), --clr-2 (azul claro), --clr-3 (escuro), --clr-4 (âmbar #fba002)
.glass utility, golden-border, dark theme
CSS nesting puro, hierarquia espelha HTML
Animações e media queries em @layer
Fontes: Saira (títulos), Jost (corpo)
IMPORTANTE: José faz customizações de estilo próprias — SEMPRE pedir o ficheiro actual antes de entregar código completo


📁 FICHEIROS A PEDIR NO INÍCIO
Para o Resend + Admin inbox, pede upload de:

admin/admin.html (actual)
admin/admin.js (actual)
admin/admin.css (actual)
user/user.html (actual — para adicionar tab/badge de mensagens)
user/user.css (actual)


31/03/2026
========================================== -->